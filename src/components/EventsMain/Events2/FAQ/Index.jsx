import React, { Component } from 'react';
import FAQBoard from "./faqs"

export default class FAQSection extends Component {
    render(
    ) {
        // console.log(this.props.data)
        return (
            // <FAQBoard faqList={this.props.data.faq} />
            <div></div>
        );
    }
}

